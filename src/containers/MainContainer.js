import React from 'react';
import CounterContainer from './CounterContainer';

const MainContainer = () => {
    return (
        <div>
            <CounterContainer />
        </div>
    );
}

export default MainContainer;
