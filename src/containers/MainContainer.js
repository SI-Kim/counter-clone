import React from 'react';
import Header from '../layouts/Header';

const MainContainer = () => {
    return (
        <div>
            <Header />
        </div>
    );
}

export default MainContainer;
